Cross-cutting integration tests (spanning `apps/api` and `apps/web`) go
here in a later phase. Unit tests for each app live alongside that app
(`apps/api/tests`, and `apps/web` once component tests are added).
